# Server-side email configuration (Alibaba DirectMail SMTP or SMTP-compatible)
# Copy to .env.server and fill real values

SMTP_HOST=smtpdm.aliyun.com
SMTP_PORT=465
SMTP_USER=your_smtp_user
SMTP_PASS=your_smtp_password
MAIL_FROM=verified-sender@your-domain.com
MAIL_TO=christinewx3@outlook.com
PORT=8080
